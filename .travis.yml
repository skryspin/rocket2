language: python
python:
    - "3.7-dev"
install:
    - pip install pipenv
    - pipenv install --dev
script:
    - pipenv run pycodestyle .
    - pipenv run pydocstyle .
    - pipenv run markdownlint .
    - cd tests/ && pipenv run pytest --cov=./
    - pipenv run codecov
